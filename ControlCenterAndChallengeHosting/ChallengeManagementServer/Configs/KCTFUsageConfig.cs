namespace ChallengeManagementServer.Configs
{
    public class KCTFUsageConfig
    { 
        // number of cpu cores kctf node using
        public static long CPUAllocatable = 0;

        // RAM (Mi) kctf node using
        public static long MemoryAllocatable = 0;
    }
}
